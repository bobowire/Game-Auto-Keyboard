# Phase 3 语音控制调试指南

## 已添加全链路日志

所有语音处理步骤都会输出到：
- **控制台** (debug 构建时可见)
- **voice_debug.log** (工作目录下，release 也可用)

## 日志输出内容

### 语音流水线 ([voice])
1. `[voice] 唤醒 score=0.xxx，开始听指令` — 检测到唤醒词
2. `[voice] 回溯补齐 N 样本(X.XX秒)` — 从环形缓冲回溯取指令开头
3. `[voice] VAD 检测到静音，录音结束` — 语音活动检测完成
4. `[voice] 指令音频 N 样本(X.XX秒)，开始 ASR 识别...` — 准备调用百度 API
5. `[voice] ASR 识别结果: 「xxx」` — 百度返回的文本（含可能的识别错误）

### 意图解析 ([intent])
6. `[intent] 原始文本: 「xxx」` — 收到的 ASR 文本
7. `[intent] 当前窗口名: [...]` — 8 个槽位的自定义名（用于匹配）
8. `[intent] 匹配: 窗口 N 执行动作「xxx」` — 成功解析出窗口 + 动作
9. `[intent] 窗口 N(窗口名) 已添加脚本: [...]` — 该窗口里可用的脚本列表
10. `[intent] 动作「xxx」匹配到脚本「xxx.ag」，启动` — 找到匹配脚本
11. `[intent] 已启动窗口 N 的脚本「xxx.ag」` — 最终执行成功

### 可能的错误
- `[voice] 超时：3秒内未检测到语音，放弃` — 唤醒后没说话
- `[voice] ASR 识别失败: xxx` — 百度 API 调用出错
- `[intent] 未匹配到任何窗口名或停止指令` — 识别文本里没有窗口名
- `[intent] 动作「xxx」在窗口 N 的脚本中未找到匹配` — 没有包含动作关键词的脚本

## 测试步骤

1. **启动程序** (debug 模式可看控制台；release 只能看日志文件)
   ```bash
   cargo run
   ```

2. **开启语音**
   - 点击工具栏 "⚙ 语音设置"，确认 API Key/Secret Key 已填写
   - 确认唤醒词模型 `wakeword_model.rpw` 存在（绿色 ✓）
   - 点击 "🎤 语音: 关" 切换为 "🎤 语音: 开"
   - 底部状态应显示 "🎤 语音待命：说\"小助手\"唤醒"

3. **准备测试环境**
   - 确保窗口1已：
     - 抓取了实际窗口（显示窗口标题）
     - 添加了包含"跟随"的脚本（如 `跟随.ag`）
   - 可以编辑窗口名：点击标题行的 "窗口1" 文本框改名

4. **说指令**
   ```
   "小助手，窗口1跟随我"
   ```

5. **查看日志**
   ```bash
   cat voice_debug.log
   # 或者 Windows 下用记事本打开 voice_debug.log
   ```

## 典型问题排查

### 问题1：底部显示"已唤醒"后无响应
**可能原因**：VAD 没检测到语音结束（连续说话 or 环境噪音）
**日志特征**：有 `[voice] 唤醒`，但没有 `[voice] VAD 检测到静音`
**解决**：说完指令后停顿 1 秒，让 VAD 检测到静音

### 问题2：ASR 识别文本和你说的不一样
**日志特征**：
```
[voice] ASR 识别结果: 「小助手人1跟随我」  ← "窗口"被听成"人"
[intent] 未匹配到任何窗口名或停止指令
```
**解决**：
- 重新训练唤醒词，说清楚一些
- 或改窗口名为"主号"等容易识别的词
- 或在日志里看百度听成了什么，调整你的发音

### 问题3：匹配到窗口但没找到脚本
**日志特征**：
```
[intent] 匹配: 窗口 1 执行动作「跟随我」
[intent] 窗口 1(窗口1) 已添加脚本: ["加血.ag", "回城.ag"]  ← 没有"跟随.ag"
[intent] 动作「跟随我」在窗口 1 的脚本中未找到匹配
```
**解决**：先在 UI 里点 "窗口1" → "+ 添加方案" → 选择包含"跟随"的脚本

### 问题4：找到脚本但没执行
**日志特征**：
```
[intent] 动作「跟随我」匹配到脚本「跟随.ag」，启动
[intent] start_slot 返回 false（窗口失效/无标识方案），未执行
```
**解决**：窗口1没抓取窗口 or 窗口已关闭失效，重新抓取

## 日志文件位置

**voice_debug.log** 在启动程序的工作目录下（一般就是项目根目录）

清空重测：
```bash
rm voice_debug.log
cargo run
# 执行测试后
cat voice_debug.log
```
