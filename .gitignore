# Rust
/target/
/build/
**/*.rs.bk
*.pdb
# 注意：二进制项目应提交 Cargo.lock 以锁定依赖版本
# （rustpotter 依赖 candle 对 half 版本敏感，锁定避免漂移导致编译失败）

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# 用户配置和状态文件
config.toml

# Windows
Thumbs.db
Desktop.ini

# 构建产物
*.exe
*.dll
*.so
*.dylib

# 测试生成的截图
*.ppm

# 语音测试产物（每人声音不同，运行时生成）
*.rpw
/wakeword_samples/
/sendvoice/
command_*.wav
config.json

# Claude Code（个人权限/会话状态，不入库）
/.claude/

# 构建/部署运行时目录（exe + assets + scripts + config.json，由构建脚本生成）
/GameAutoKeyboard/
